import React from "react";

const App = () => {
	return (
		<div
			style={{
				display: "grid",
				minHeight: "100vh",
				placeItems: "center",
			}}
		>
			<h1 className="text-3xl font-bold underline text-primaryColor">Hello world!</h1>
		</div>
	);
};

export default App;
