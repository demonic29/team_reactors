import React from "react";
import { FiEdit as FiEditIcon } from "react-icons/fi";

const FiEdit = ({ size = 35, color = "white" }) => {
	return <FiEditIcon size={size} color={color} />;
};

export default FiEdit;
